<view class="devices_summary">
  <text>已发现 {{devices.length}} 个设备：</text>
</view>
<scroll-view class="device_list" scroll-y scroll-with-animation>
  <view
    wx:for="{{devices}}"
    wx:key="index"
    data-device-id="{{item.deviceId}}"
    data-device-name="{{item.name || item.localName}}"
    bind:tap="{{connectedId === item.deviceId ? 'closeBLEConnection' : 'createBLEConnection'}}"
    class="device_item"
    hover-class="device_item_hover"
  >
    <view class="device_item__inner">
      <text>{{item.name}}</text>
      <text wx:if="{{connectedId === item.deviceId}}" class="weui-badge">已连接</text>
      <text wx:else class="weui-badge" style="background-color: darkgrey">未连接</text>
    </view>
  </view>
</scroll-view>
